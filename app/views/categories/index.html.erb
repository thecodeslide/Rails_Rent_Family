<div class="h1-index-category">
  <h1>Services</h1>
</div>
<div class="cards-category-index">
  <% @categories.each do |category|  %>
  <%= link_to category_path(category), class: 'category-cards-no-link-styles' do %>
  <div class="card-product">
    <%= cl_image_tag(category.photo.key, width: 300, height: 300, crop: :fill) %>
    <div class="card-product-infos">
      <h2>Age: <%= category.name  %></h2>
      <p>Description: <%= category.description %></p>
      <p>￥<%= category.price %></p>
      <div class="d-flex justify-content-end">
        <%# if policy(category).edit? %>
        <%# end %>
      </div>
    </div>
  </div>
  <% end %>
  <% end %>
</div>
